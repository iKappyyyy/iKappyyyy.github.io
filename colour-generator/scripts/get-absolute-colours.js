/*
const intervalID = setInterval(() => {
  colours = generateColours();
  if (coloursInGrid(colours)) {
    generateHTML(copyType, colourAmount, colours);
    clearInterval(intervalID);
  }
}, 1);

function coloursInGrid(colours) {
  colours.forEach((row) => {
    row.forEach(col => {
      if (col.rgb === '(0, 0, 0)' || col.rgb === '(255, 255, 255)') return true;
    });
  });
  return false;
}
*/